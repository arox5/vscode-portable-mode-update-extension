# Project-specific npm configuration for VS Code extension
# This will override global and user-level .npmrc settings

# Use the default npm registry instead of corporate registry
registry=https://registry.npmjs.org/

# Enable strict SSL (good practice for public registry)
strict-ssl=true

# Optional: Set specific configurations for this project
# save-exact=true
# engine-strict=true
